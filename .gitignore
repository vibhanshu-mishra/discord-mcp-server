node_modules/
dist/

# Secrets & environment — never commit real tokens or local env files.
.env
.env.*
!.env.example

# Local data, databases, backups, exports and logs — may contain server/member data.
data/
database/
backups/
exports/
logs/
*.db
*.db-journal
*.sqlite
*.sqlite3
*.lock

# MCP bundle build staging and generated artifacts (never commit the .mcpb).
.mcpb-build/
artifacts/
*.mcpb

# OS / editor cruft
.DS_Store

# Local Claude Code config & artifacts
.claude/

# Local-only files (not part of the published package)
.mcp.json

# Local audit / review artifacts
AUDIT_VERIFICATION.md
