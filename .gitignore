node_modules/
dist/

# Secrets & environment — never commit real tokens or local env files.
.env
.env.*
!.env.example

# Local data, databases, exports and logs — may contain server/member data.
data/
database/
exports/
logs/
*.db
*.db-journal
*.sqlite
*.sqlite3

# OS / editor cruft
.DS_Store

# Local Claude Code config & artifacts
.claude/

# Local-only files (not part of the published package)
.mcp.json

# Local audit / review artifacts
AUDIT_VERIFICATION.md
